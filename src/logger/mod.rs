//! Terminal output formatting and colored display.
//!
//! Provides user-facing output functions (write, success, error, info,
//! warning) that use the `colored` crate for ANSI terminal colors.

pub mod formatter;
